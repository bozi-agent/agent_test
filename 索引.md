# 索引 —— AI 资讯工作台（DSH 插件）

> 给 AI 助手看的导航图。**改代码前先读这份，不要全库通读。**
> 行号会随改动漂移，定位时用下面的"函数名"搜，别死记数字。
> 最后核对时间：2026-09-18（对应当前工作区代码）

---

## 0. 一句话

DSH 侧边栏插件，一个全屏工作台，5 个模块走完：
**① 选题库 → ② 扫源抓取 → ③ 筛选核实 → ④ 写文案 → ⑤ 封面标签**。
界面侧（浏览器）和主机侧（Node）靠一个 HTTP 接口 `/xmt-kf/api` 说话。数据全落在 `~/.dsh/ai-news-workbench/`。

> **⚠ 已移除的模块**：原来的「⑥ 发布与互动」「⑦ 数据复盘」**已按需求彻底删掉** ——
> 界面（卡片、二级页、按钮、状态、请求）和主机侧（`publishList` / `comments` / `pickFile` /
> `parseExcel` / `analyzeReview` 五个接口）**全都没了**，`lib/review.js`、`lib/xlsx.js` 两个文件
> 整个删除，`test/make-sample-xlsx.mjs`（示例报表生成器）也一并删了；
> `review-data.json` 的读写、`defaultStatus()` 里的 `module6`/`module7`、`generate.js` 里的
> `generatePublishList`/`generateComments` 同样清掉了。**现在没有孤儿文件了。**

```
浏览器（lib/client/ → 打包成 lib/client-dist/client.js）
        │  fetch POST  /xmt-kf/api   { action, payload }
        ▼
主机（lib/index.js 里的 dispatch）  ← 唯一的路由入口
        ├── lib/crawl.js    抓网页
        ├── lib/filter.js   筛选判断
        ├── lib/rules.js    文案规则 / 去 AI 味 / 检查清单
        ├── lib/generate.js 本地文案生成
        ├── lib/llm.js      大模型调用（**当前只有「AI 润色」还在用**）
        └── lib/store.js    读写磁盘 JSON
```

---

## 1. 文件清单（按重要性）

| 文件 | 行数 | 干什么 |
|---|---|---|
| `lib/index.js` | ~1476 | **主机侧全部**：路由、所有 action、模板文件夹、图片读取 |
| `lib/client/workbench.js` | ~2130 | **界面侧全部**：工作台弹窗、5 个模块的界面、设置页、所有按钮动作 |
| `lib/crawl.js` | ~1580 | 抓网页：解析列表、读详情页、提日期、提正文 |
| `lib/client/ui.js` | ~1396 | 界面零件：CSS 变量、整张样式表、小图标、请求封装 |
| `lib/rules.js` | ~590 | 文案规则：清洗文本、高亮事实、去 AI 味、违禁词、人设注入 |
| `lib/store.js` | ~441 | 主机侧读写磁盘 + 默认设置 + 默认状态 |
| `lib/generate.js` | ~199 | 本地规则生文案（不调模型时的兜底） |
| `lib/llm.js` | ~158 | 调大模型（OpenAI 兼容 `/chat/completions`） |
| `lib/client/client.js` | ~538 | 界面侧入口：注册侧边栏按钮 + 挂弹窗 |
| `lib/client/sidebar-entry.js` | ~144 | 把入口按钮搬到「技能中心」正下方 |
| `lib/client/store.js` | ~85 | 界面侧小状态仓（只管弹窗开/关） |
| `build/build-client.mjs` | ~126 | 把 `lib/client/` 拼成浏览器能吃的 bundle |
| `test/*.mjs` | — | 测试，见第 6 节 |

**⛔ 不要读的文件**
- `lib/client-dist/client.js`（~4400 行）：**打包产物**，由 `build/build-client.mjs` 生成。读它没有意义，改了也会被覆盖。
- `xmt_kf/assets/templates/*.jpg`：模板图片，二进制。

---

## 2. 主机侧 `lib/index.js`

### 2.1 路由（先看这里）
- `createHandler(ctx)` —— 唯一的 HTTP 处理函数。GET 走 query 参数、POST 走 JSON body。
- `dispatch(ctx, action, payload)` —— **所有 action 的大 switch**。加新接口就在这里加一个 `case`，再去同文件写一个 `actionXxx()`。
- `API_PREFIX = '/xmt-kf/api'`、`rejected(ctx, req, res)`（鉴权）、`sendJson`、`readBody`。
- `apply(ctx)`（文件末尾）注册路由；`inject = ['webServer']` —— **只声明 webServer**（刻意不声明 llm）。

### 2.2 action 一览（名字 → 函数 → 大致行号）

| action | 函数 | 干什么 |
|---|---|---|
| `status` | `actionStatus` | 一次性返回所有计数（界面刷新主入口） |
| `settings` / `saveSettings` | `loadSettings` / `saveSettings` | 设置读写 |
| `load` | `actionLoad` | 批量读数据 |
| `saveTopic` / `deleteTopic` | `saveTopic` / `deleteTopic` | 选题库增删 |
| `fetch` | `actionFetch` | 抓取（进度写在 `fetchProgress`） |
| `fetchProgress` | `readProgress` | 抓取进度轮询 |
| `filter` | `actionFilter` | 筛选打分选候选 |
| `confirm` | `actionConfirm` | 核实通过 |
| `writeCopy` / `writeFromTopics` | `actionWriteCopy` / `actionWriteFromTopics` | 生成文案 |
| `polish` | `actionPolish` | **唯一还在调大模型的接口** |
| `updateCopy` / `deleteCopy` | `actionUpdateCopy` / `actionDeleteCopy` | 改 / 删文案 |
| `deAi` | `actionDeAi` | 去 AI 味（纯本地规则） |
| `listTemplates` | `listTemplateFiles` | 列出模板文件夹里的图片 |
| `openTemplateFolder` | `openTemplateFolder` | 唤起资源管理器 |
| `useTemplate` | `actionUseTemplate` | 选定模板 + 读尺寸 |
| `templateImage`（**GET 专用**） | `sendTemplateImage` | 直接吐图片字节流，不套 JSON |
| `storage` | `storageInfo`（在 store.js） | 磁盘占用 |

（原来还有 `publishList` / `comments` / `pickFile` / `parseExcel` / `analyzeReview` 五个，随「⑥ ⑦」一起删了。）

### 2.3 模板文件夹（模块5 的核心，踩过坑）
- `PLUGIN_DIR` —— 插件自己的目录。
- `templateDirCandidates()` / `templateDir()` / `hasImages(dir)` —— **优先用插件下 `assets/templates/`，老位置 `~/.dsh/ai-news-workbench/templates/` 有图也认**。改路径先看这里。
- `listTemplateFiles()` / `templateInfoOf(settings)` / `templateDirOf(file)` —— 列清单、取当前模板、防路径穿越。
- `openTemplateFolder(preferFile)` —— ⚠ **曾经的 bug**：这里原来写 `require('node:child_process')`，但本文件是 ESM、没有 `require`，一调用就抛错又被 try/catch 吞掉，表现是"点了没反应"。现在用顶层 `import { spawn, spawnSync }`，并走 PowerShell（已有窗口就拉到前台，没有才新开）。
- `readImageSize(buffer, file)` —— 自己读 png/jpg/webp/gif 的宽高，不依赖第三方。
- `psQuote` / `runDetached` / `runPowerShell` —— 起外部进程的三个小工具。

---

## 3. 界面侧 `lib/client/workbench.js`

### 3.1 结构与导航
- `MODULES` —— 5 个模块的定义（id / 编号 / 名字 / 图标 / 状态栏文案）。**改模块名或状态栏文案只改这里。**
  - `m1` 选题库 / `m2` 扫源抓取 / `m3` 筛选核实 / `m4` 写文案 / `m5` 封面标签
  - （原来的 `m6` 发布与互动 / `m7` 数据复盘已删除；`bodyFor()` 末尾留了一个 `return h(Empty)` 兜底）
- `createWorkbench(react)` —— 主组件工厂，里面所有东西都在这个闭包里。
- 一级页 = 模块卡片网格；二级页 = 某个模块的详情。靠 `screen` + `moduleId` 两个 state 切换。
- **每个模块的界面按 `if (module.id === 'mX') { ... }` 分块**（搜 `if (module.id ===`）：
  | 位置 | 内容 |
  |---|---|
  | 状态栏文案 | `getStatus`（在 MODULES 里）+ 动态覆盖在 ~1183 行 |
  | 状态栏颜色 | ~1201 行（TONE.done / idle / run） |
  | 卡片小数字 | ~1224 行 |
  | 右上角按钮组 | ~1274 行 |
  | **模块主体界面** | 都在 `bodyFor()` 里（搜 `const bodyFor`），按 `if (module.id === 'mX')` 一段段排；`m5` 是最后一段（搜 `封面标签里有「删除」按钮`） |

### 3.2 动作函数（`useCallback` 闭包，都在 `createWorkbench` 里）
| 函数 | 干什么 |
|---|---|
| `refreshAll` / `refreshStatus` | 拉主数据 / 只拉计数 |
| `enterModule` | 进二级页 |
| `syncSavedFromTopics` | 选题库 → 同步已存状态 |
| `doFetch` / `doFilter` / `doConfirmOne` | 模块2 / 3 |
| `doWriteCopy` / `doWriteFromTopic` / `doPolish` / `doUpdateCopy` / `doDeleteCopy` | 模块4 |
| `doOpenTemplateFolder` / `doUseTemplate` / `doAddTag` / `doDeleteLayout` | 模块5 |
| `doLayoutRefresh` / `doLayout` | 模块5 刷新（**先从主机侧重读写文案的稿子**，否则刚保存的帖子看不见） |
| `doSaveTopic` / `doDeleteTopic` / `doSaveSettings` / `doLoadLog` | 通用 |
| `buildTagsLocal(title, body)` | **本地**提标签（主题词表 + 英文专名 + 中文高频词），不调模型 |
| `flash(text)` | 顶部提示（写进 `notice`，各页面**必须自己渲染 `CLS.toast`** 才看得见，见 3.6） |

### 3.3 进模块自动刷新
搜 `autoRefreshedFor` —— 进 **m1 / m3 / m4 / m5** 时各自动刷一次；用 `useRef` 记账，同一屏只刷一次（否则会因数据变化反复刷）。

### 3.4 界面零件
`Btn` / `TagEditor` / `CopyButton` / `Card` / `CheckRow` / `Hint` / `Empty` / `Badge` / `Metric`。

### 3.5 「⑤ 封面标签」模块（改动最频繁，先说清楚它的现状）
界面按 `if (module.id === 'm5')` 分块，搜 `封面标签里有「删除」按钮` 附近的注释能定位。当前形态：

- **一条帖子 = 左右两栏**：左栏封面（`CLS.split` / `splitLeft` / `splitRight`），右栏标题 / 正文 / 标签；
  窄屏由 CSS 的 `@media (max-width:760px)` 自动折成上下两段。
- **正文完整显示**：用 `CLS.bodyFull`，**不要**用 `CLS.bodyClamp`（那是模块3 的省略号版）。
- **「删除」按钮在右下角**：`CLS.itemFoot`（`justify-content:flex-end`）。
- **封面画在每条帖子自己的左栏里**，不再有"脱离帖子的独立封面预览块"——
  以前那块是独立画的，所以删掉帖子后封面还在屏幕上（用户报过这个 bug，别改回去）。
- **标签 4~6 个**：`buildTagsLocal` 的三个上限（英文 6 / 中文 6 / 兜底 4）和 `slice(0, 6)`。

**三条踩过的坑（别改回去）**
1. **`buildTagsLocal` 每次渲染返回新数组**，所以 `TagEditor` **绝不能**拿 `tags` 的引用当"变没变"的判据
   （老代码 `useEffect(..., [tags])` 每次都 `setDraft` → 渲染风暴 → 页面卡死、点什么都没反应）。
   现在 `TagEditor` 把标签内容拼成字符串比。
2. **`doLayoutRefresh` 里 `api.post('layout')` 必须 `.catch`**：没有保存过的帖子时主机侧返回 `ok:false`，
   接口层会抛错并跳过后面"拉模板清单"那段 → 模板缩略图永远是空的（"点哪张都没反应"）。
3. **`busy` 看门狗**：所有按钮都是 `disabled: !!busy`，任何动作的 Promise 不返回就会把整页锁死。
   有一个 30 秒的 `useEffect` 兜底强制解锁（依赖里带了 `progress`，所以正常抓取不会被打断）——
   如果看到"操作卡住了"的提示，说明真有人卡住了，去查根因。

### 3.6 设置页 `pageSettings`（搜 `const pageSettings`）
- 卡片顺序：`1) 模型（写文案 / AI 润色）` → `2) 网站清单` → `3) 抓取参数` → `4) 筛选规则关键词` → 可延展门槛。
- **保存 / 返回只在顶部那一条**（`CLS.topbar`）里；**页面底部那对按钮已按需求取消**。
- **网站清单每条**：左边 `CLS.fieldBox`（名称 / 网址 / 抓取难度）+ 右边 `CLS.siteRowAction` 放「删除这个网站」，
  外层是 `CLS.siteRow`（flex）——这样按钮稳定停在右上角、紧挨「抓取难度」。
- ⚠ **提示浮层必须各页面自己渲染**：`error`/`notice` 是全局 state，但每个页面各自往 `<div class="xmt-toast">` 里放。
  设置页原来**只渲染 error 不渲染 notice**，所以「设置已保存」在一级页看得见、进设置页就没了（用户报过）。
  以后加新页面记得把这段一起写上。`.xmt-toast` 是 `position:fixed; z-index:30`，比顶栏的 sticky `z-index:5/6` 高，不会被盖。

---

## 4. 抓取 `lib/crawl.js`（最复杂的一块）

| 环节 | 关键函数 |
|---|---|
| 取网页 | `fetchText` / `politeDelay` / `browserFetchText`（可选走 CDP） |
| 解析 HTML | `findTag` / `innerHtmlOf` / `queryAll` / `parseSelector` / `stripTags` / `decodeEntities` |
| 找列表 | `parseList` / `parseArticles` / `parseRepeatedList` / `scoreArticleList` / `isNavLike` |
| 提标题 | `cleanTitle` / `compactTitle` |
| **提日期** | `findDate`（`<time>` → meta → JSON-LD → 时间容器 → 中文"发布时间：" → 全文兜底）、`detectDate`、`jsonLdDate`、`plausibleDate`、`withinRecentDays` |
| **提正文** | `extractBodyText` → `pickContentBlock` → `dropNoiseBlocks` → `htmlToLines` → `elementRange` |
| 主流程 | `crawlSite`（单个站）/ `crawlAll`（全部） |

**两个已修的老坑（别改回去）**
1. `VOID_TAGS` + `elementRange`：`<img>`、`<br>` 这类自闭合标签以前会让"元素范围"算不到结尾，把整页正文当噪音删光 → 正文全空。
2. 日期兜底扫描以前会捡到侧栏里别人的日期（所以"选今天却抓出 2 天前"）。现在只认"带时分"的串。

---

## 5. 数据落盘 `lib/store.js`

- `DSH_HOME` → `DATA_DIR = ~/.dsh/ai-news-workbench/`
- `FILES`：`settings.json` / `topic-library.json` / `fetch-cache.json` / `drafts.json` / `status.json` / `fetch-log.txt`
  （`review-data.json` 随「⑦ 数据复盘」一起删了）
- `defaultSettings()` —— **所有默认值和字段含义都在这**（网站清单、抓取参数 `fetch.days`、模型配置、筛选关键词表…）
- `defaultStatus()` —— 各模块的状态字符串（现在只有 `module2`…`module5`）
- `loadDrafts()` / `saveDrafts(patch)` —— 中间产物（候选 / 文案 / 排版 / 已删名单）；**局部更新**，只覆盖传进去的字段
- `readJson` / `writeJson` / `withRetry`（原子写 + 重试）/ `tailLog`
- ⚠ 设置里还留着 `skillText` / `skillTextLayout` / `modelLayout` 三个**已经废弃**的字段（做图排版改成本地工具后没人用了），删之前先确认没别的地方读。

---

## 6. 测试 `test/`

跑法：`cd xmt_kf && node build/build-client.mjs` 再逐个跑；`npm test` 一次跑全。

| 文件 | 测什么 |
|---|---|
| `smoke.mjs` | 主机侧冒烟（存储层读写、抓取解析、硬规则、本地生成） |
| `integration.mjs` | 主机接口端到端（假 ctx + 真 HTTP + 本地假新闻站） |
| `store.mjs` | 界面侧小状态仓（**快照必须引用稳定**，否则 React #185 无限重渲染） |
| `sidebar-entry.mjs` | 侧边栏入口的落位与样式 |
| `ui.mjs` | 界面组件（用 `_harness.mjs` 的假 react 渲染后断言） |
| `style.mjs` | 样式表（`ensureStyles` 往页面注入的那份） |
| `bundle-check.mjs` | 打包格式（必须是 `__ModuleLoader__.load({factory})`，不能有 import/export） |
| `combo-check.mjs` / `e2e-boot.mjs` | 按浏览器里的加载顺序整体跑一遍，抓"Failed to load plugins" |
| `_harness.mjs` | 公共脚手架：假 react + 假接口 + 渲染器 + 断言 |

**⚠ 脚手架的 `useMemo` / `useCallback` 是残的**（`test/_harness.mjs`）：
`useMemo(fn)` 只认第一个参数，**完全忽略依赖数组**——只在第一次渲染时算，之后永远返回缓存值。
所以**别把"会被 setState 影响的结果"放进 `useMemo`**：真浏览器里正常，测试里永远不更新，
会看到"改了状态界面却没变"的假失败。同理 `useCallback` 也忽略依赖。
（`useEffect` 在非 probe 模式下是空操作，只有 `probe()` 打开时才真的执行。）

---

## 7. 改完必须做的两步

1. **改 `lib/client/` 下任何文件后**：`node build/build-client.mjs`（重新打包），**然后重启 DSH + 浏览器 Ctrl+F5**。只改 `lib/*.js`（主机侧）也要重启 DSH。
2. 客户端 bundle 是"注册工厂"格式，**源码里出现 `import`/`export` 没被打包掉，会让排在它后面的所有客户端插件一起挂**，页面报 `Failed to load plugins`。所以别手改 `client-dist/client.js`。

---

## 8. 已知的"改动前就红"的老测试

`ui.mjs` 约 10 项、`style.mjs` 约 4 项、`bundle-check.mjs` 1 项长期失败 —— 都是**弹窗几何用 `calc()` 量不到数值**那批，跟新改动无关。动代码前先确认基线，别把老红当成自己引入的。

---

## 9. 测试会往哪儿写文件（踩过）

- `integration.mjs` 的「⑤ 封面标签」那节会往**真实的模板文件夹**（`xmt_kf/assets/templates/`）里写
  `tpl-a.png` / `tpl-b.jpg` / `readme.txt`。这地方同时是**用户自己放模板的地方**，所以：
  - 断言必须写成"我们放进去的都在清单里"，**不能**断言"清单正好 2 条 / 文件夹是空的"
    （用户往里面放了自己的图之后，以前那两条断言就一直红）；
  - 测完必须自己把造的文件删掉，否则会以"坏缩略图"的形式出现在用户的模板选择里。
- 其余测试都写在 `TEMP_HOME` 临时目录里，不碰用户数据。
