# ============================================================================
# dsh-ai-news-workbench —— DSH bundle patch（挂载声明）
# ============================================================================
# 把这个"AI 资讯工作台"插件插入 web profile 的配置树。
# 安装（本机开发用 link 方式，不发布 npm）：
#   dsh plugin --profile web add link:<本插件的绝对路径>
# 例如：dsh plugin --profile web add link:D:\code\dsh-ai-news-workbench
# 装完重启 dsh web 生效。
# ============================================================================

- insert:
    - id: dsh-ai-news-workbench
      name: dsh-ai-news-workbench
